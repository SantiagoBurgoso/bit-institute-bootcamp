// CONDICIONALES

// Ejemplo 1: if y else simple
let roadCondition = 'muddy';
if (roadCondition === 'clear') {
    console.log('You can ride safety.');
} else {
    console.log('Caution, there is mud. Ride slowly to avoid an accident.');
}

// Ejemplo 2: if, else if y else
    let studyModule ='JavaScript';
    if (studyModule === 'HTML') {
        console.log('You are learning HTML.');
    } else if (studyModule === 'JavaScript') {
        console.log('You are learning JavaScript.');
    } else {
        console.log('You are learning a different module.');
    }